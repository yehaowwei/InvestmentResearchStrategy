# Convertible Board Bootstrap Resources

These files are only used as the initial bootstrap source for the default `转债看板` group.

The canonical runtime upload directory is:
`.runtime/external-resources/<group-slug>/`

Use the `外部资源配置` page to create groups and upload new HTML resources.
